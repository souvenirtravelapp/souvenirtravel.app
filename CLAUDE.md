# سوفينير — الويب

قبل أي عمل يمس الواجهة أو التصميم أو الحركة أو النصوص المرئية:
اقرأ `~/Projects/souvenir/SOUVENIR-APP-DESIGN.md` والتزم به،
وافحص عملك على قائمته (§8) قبل التسليم.
قبل أي دفع يمس تطبيق الويب: شغّل `./bump.sh` أولًا، ثم احفظ ختمه الجديد في
`app/index.html` **ضمن** الـ commit — فالختم خارج اللقطة يعني شيفرةً جديدة
بوسمٍ قديم، والزائر يبقى على المخبّأ عنده.

## الحفظ في git — قد تعمل جلسة أخرى هنا الآن

أكثر من جلسة تعمل في مستودعات سوفينير معًا، ولا ترى إحداها الأخرى. فالتزم بالأربع:

1. **أضف ملفاتك بأسمائها**: `git add <المسار>` لكل ملف عدّلته أنت.
   ممنوع `git commit -a` و`git add .` و`git add -A` — تبتلع عمل جلسةٍ أخرى
   وتضمّه إلى commit لا يصفه. وقع فعلًا في `5ff40d1` يوم 2026-09-02.
2. **اقرأ `git status` قبل كل حفظ.** ما لم تعدّله أنت اتركه كما هو، ولو بدا
   غريبًا أو ناقصًا — فصاحبه يعمل عليه في هذه اللحظة.
3. **احفظ فور اكتمال وحدة العمل، لا في آخر الجلسة.** إذنٌ قائم من طارق
   (2026-09-02): احفظ بلا استئذان، فما لم يُحفظ لا نقطة رجوع له. والدفع
   وحده يبقى بكلمته.
4. **لا تعِد كتابة تاريخ `main`**: لا `rebase` ولا `reset --hard` ولا `amend`
   على عملٍ دُفع أو قد يكون لجلسة أخرى.
